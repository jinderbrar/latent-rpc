{
  "name": "token-wire-node-benchmark",
  "version": "1.0.0",
  "description": "Node.js V8 headless benchmark runner for TokenWire — no browser, no DOM.",
  "type": "module",
  "scripts": {
    "bench": "tsx cli.ts",
    "bench:decode": "tsx cli.ts --decode",
    "list": "tsx cli.ts --list-datasets"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.12",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
